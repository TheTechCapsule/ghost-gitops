apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: default
resources:
  - ../../base
  - ingress.yaml
  - hpa.yaml

patches:
  - path: patch-ghost-perms.yaml
  - path: patch-ghost-probes.yaml
    target:
      kind: Deployment
      name: ghost